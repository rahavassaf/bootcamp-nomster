<%= render partial: 'render_slider' %>

<%= render partial: 'render_pagination' %>

<% @places.each  do |place| %>
	<%= render partial: "render_place", locals: {place: place, rating: @ratings[place.id], comment: true} %>
<% end %>

<%= render partial: 'render_pagination' %>