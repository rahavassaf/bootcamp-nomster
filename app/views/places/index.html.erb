<%= render partial: 'render_places_index' %>

<% if user_signed_in? %>
	<%= link_to '#', data: {toggle: 'modal', target: "#new_place_modal"} do %>		
		<div class="plus_button accent-color active_link hover-scale" data-toggle="tooltip" title="New Place">
			<i class="fa fa-plus"></i>
		</div>
	<% end %>
<% end %>